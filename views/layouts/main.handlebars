<!DOCTYPE html>
<html>
    <head>
        <title>Seattle Sports</title>
        <style>
        @import url('https://fonts.googleapis.com/css?family=Oswald:300|Permanent+Marker');

        body {
            background: #9f9f9f;
            margin: 150px 0;
            text-align: center;
            color: #fff;
            font-family: 'Oswald', sans-serif;

        }

        header {
            filter: drop-shadow(5px 5px 5px #222);
            margin-bottom: 2.5em;
            letter-spacing: 1.5em;
        }

        ul, li {
            list-style-type: none;
        }

        h1 {
            font-family: 'Permanent Marker', sans-serif;
        }

        #btn {
            color: #777;
            text-shadow: 0 1px 0 rgba(255,255,255,.8);
            background: #fff;
            display: inline-block;
            min-width: 110px;
            padding: 5px 7px 6px;
            font: 700 12px/1 'Oswald', sans-serif;
            border-radius: 3px;
            box-shadow: 0 10px 20px rgba(0,0,0,.1);
        }

        .card {
            margin: auto;
            padding: 0 1.7rem;
            width: 25%;
        }

        .wrapper {
            background-color: #fff;
            min-height: 400px;
            position: relative;
            overflow: hidden;
            box-shadow: 0px 5px 10px #888888;;
            background-size: 100%;
            height: 500px;
            background-repeat: no-repeat;
            
        }
        
        .data {
            position: absolute;
            bottom: 0;
            width: 100%;
            
        }
  
        .author {
            font-size: 12px;
        }

        .title {
            margin-top: 10px;
        }

        .text {
            height: 70px;
            margin: 0;
        }
  
        .content {
            background-color: #fff;
            box-shadow: 20px 5px 5px rgba(#000, 0.8);
            color: rgb(14, 29, 71);
        }

        .title a {
            color: lighten(#000, 50%);
        }

    </style>
    </head>
    <body>
        <header>SEATTLE</header>
        {{{body}}}
    </body>
</html>